import { render, screen } from '@testing-library/react';

import Banner from '.';

describe('<Banner />', () => {
  it('should render correctly', () => {
    // Veriricar se o title existe renderizado
    // Verificar se o subtitle existe renderizado
    // Verificar se a imagem existe renderizada
  });
});
