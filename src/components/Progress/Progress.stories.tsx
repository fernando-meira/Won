import { Meta, Story } from '@storybook/react/types-6-0';

import Progress from '.';

export default {
  title: 'Progress',
  component: Progress,
} as Meta;

export const Basic: Story = () => <Progress />;
